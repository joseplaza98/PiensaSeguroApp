import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SegRedFormularioPage } from './seg-red-formulario';

@NgModule({
  declarations: [
    SegRedFormularioPage,
  ],
  imports: [
    IonicPageModule.forChild(SegRedFormularioPage),
  ],
})
export class SegRedFormularioPageModule {}
